export default function Courses() {
  return (
    <section>
      <h1 className="text-xl font-semibold">Courses</h1>
      {/* ponytail: placeholder — loader fetches course list from the API */}
      <p className="text-muted-foreground">Course list goes here.</p>
    </section>
  );
}
